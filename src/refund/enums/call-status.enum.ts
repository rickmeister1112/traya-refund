export enum CallStatus {
  CONNECTED = 'connected',
  NOT_PICKED_FIRST_ATTEMPT = 'not_picked_first_attempt',
  NOT_PICKED_SECOND_ATTEMPT = 'not_picked_second_attempt',
  SCHEDULED = 'scheduled',
  CANCELLED = 'cancelled',
}

